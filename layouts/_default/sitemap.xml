{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range .Data.Pages }}
  {{- $include := false -}}
  {{- if eq .Kind "home" -}}
    {{- $include = true -}}
  {{- else if and (eq .Kind "page") (eq .Section "post") -}}
    {{- $include = true -}}
  {{- else if and (eq .Kind "page") (not .File.IsZero) -}}
    {{- $name := .File.TranslationBaseName -}}
    {{- if or (eq $name "about") (eq $name "services") (eq $name "media") -}}
      {{- $include = true -}}
    {{- end -}}
  {{- end -}}
  {{- if $include }}
  {{- $priority := "0.8" -}}
  {{- if eq .Kind "home" }}{{ $priority = "1.0" -}}
  {{- else if not (eq .Section "post") }}{{ $priority = "0.7" -}}
  {{- end -}}
  {{- $changefreq := "monthly" -}}
  {{- if eq .Kind "home" }}{{ $changefreq = "weekly" -}}
  {{- else if not (eq .Section "post") }}{{ $changefreq = "yearly" -}}
  {{- end -}}
  {{- $defaultURL := .Permalink -}}
  {{- range .AllTranslations -}}
    {{- if eq .Language.Lang "en" }}{{ $defaultURL = .Permalink }}{{ end -}}
  {{- end }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05Z07:00" | safeHTML }}</lastmod>
    {{- end }}
    <changefreq>{{ $changefreq }}</changefreq>
    <priority>{{ $priority }}</priority>
    {{- if .IsTranslated }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.LanguageCode }}" href="{{ .Permalink }}"/>
    {{- range .Translations }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.LanguageCode }}" href="{{ .Permalink }}"/>
    {{- end }}
    <xhtml:link rel="alternate" hreflang="x-default" href="{{ $defaultURL }}"/>
    {{- end }}
  </url>
  {{- end }}
  {{- end }}
</urlset>
